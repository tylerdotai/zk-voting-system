{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "out",
  "installCommand": "npm install",
  "routes": [
    { "src": "/chair", "dest": "/chair.html" },
    { "src": "/verify", "dest": "/verify.html" },
    { "src": "/404", "dest": "/404.html" }
  ]
}